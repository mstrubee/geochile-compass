# Red Comercial Nacional — ETL OSM
# Instalar: pip install -r etl/comercio_osm/requirements.txt

requests>=2.31.0
python-dotenv>=1.0.0
shapely>=2.0.0
# psycopg2-binary eliminado: el ETL ya no accede directo a la DB.
# Las escrituras van via Edge Function sync-comercio-osm (HTTP + SYNC_API_TOKEN).
